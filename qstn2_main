# Write a shell script to read a file containing a set of numbers. Store the even and odd numbers among them in two different files. 


cat num.txt | while read num
do

        if [ `echo "${num} % 2" | bc` -eq 0 ] 
        then  
                echo ${num} >> even.txt
        else
                echo ${num} >> odd.txt
        fi
done

